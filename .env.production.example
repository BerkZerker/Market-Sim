# Market-Sim Production Environment Configuration
# Copy to .env and fill in ALL required values before deploying.

# ============================================================
# REQUIRED — App will refuse to start without these in production
# ============================================================

# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=CHANGE_ME_TO_A_RANDOM_64_CHAR_HEX_STRING

# PostgreSQL connection (must match postgres service credentials)
DATABASE_URL=postgresql+asyncpg://marketsim:CHANGE_ME_DB_PASSWORD@postgres:5432/marketsim

# Your domain (used for CORS and Caddy HTTPS)
DOMAIN=marketsim.yourdomain.com

# CORS: JSON array of allowed origins (must include your domain)
ALLOWED_ORIGINS=["https://marketsim.yourdomain.com"]

# Environment mode (enables strict validation)
ENVIRONMENT=production

# ============================================================
# PostgreSQL container credentials (must match DATABASE_URL)
# ============================================================

POSTGRES_USER=marketsim
POSTGRES_PASSWORD=CHANGE_ME_DB_PASSWORD
POSTGRES_DB=marketsim

# ============================================================
# OPTIONAL — Sensible defaults are provided
# ============================================================

# JWT token expiry (hours)
# JWT_EXPIRE_HOURS=24

# Market tickers (JSON: ticker name → initial price)
# TICKERS={"FUN": 100.0, "MEME": 50.0, "YOLO": 200.0, "HODL": 75.0, "PUMP": 25.0}

# Starting cash for new users
# STARTING_CASH=10000.0

# Rate limiting (requests per window per user)
# RATE_LIMIT_REQUESTS=30
# RATE_LIMIT_WINDOW=60

# Server (usually don't change these)
# HOST=0.0.0.0
# PORT=8000

# ============================================================
# FUTURE — Uncomment when features are implemented
# ============================================================

# Stripe (for payments/tournaments)
# STRIPE_SECRET_KEY=sk_live_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# Error tracking
# SENTRY_DSN=https://...@sentry.io/...

# Database connection pool
# DATABASE_POOL_SIZE=10
# DATABASE_MAX_OVERFLOW=20
