# Market-Sim environment configuration
# Copy this file to .env and uncomment/modify values as needed.
# All settings have sensible defaults — a bare .env (or no .env) works fine.

# Database
# DATABASE_URL=sqlite+aiosqlite:///./market.db

# JWT authentication
# JWT_SECRET=<random-hex-string>   # auto-generated if unset
# JWT_ALGORITHM=HS256
# JWT_EXPIRE_HOURS=24

# Market tickers (JSON object: ticker → initial price)
# TICKERS={"FUN": 100.0, "MEME": 50.0, "YOLO": 200.0, "HODL": 75.0, "PUMP": 25.0}

# Starting cash for new users
# STARTING_CASH=10000.0

# Rate limiting (requests per window per user)
# RATE_LIMIT_REQUESTS=30
# RATE_LIMIT_WINDOW=60

# Server
# HOST=0.0.0.0
# PORT=8000
