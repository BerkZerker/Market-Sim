[project]
name = "marketsim"
version = "0.1.0"
description = "Python SDK for the Market-Sim trading API"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28",
    "websockets>=12.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
